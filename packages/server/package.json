{
  "name": "@saxo/server",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://github.com/Qrymy/saxo",
  "repository": {
    "type": "git",
    "url": "https://github.com/Qrymy/saxo",
    "directory": "packages/server"
  },
  "license": "MIT",
  "author": "Qrymy",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc",
    "build": "esbuild --bundle --outdir=dist --minify --sourcemap --format=cjs --platform=node src/index.ts",
    "predeploy": "yarn typecheck && yarn build"
  },
  "engines": {
    "npm": "please_use_yarn_instead"
  },
  "devDependencies": {
    "esbuild": "^0.8.31"
  },
  "dependencies": {
    "firebase-admin": "^9.4.2",
    "firebase-functions": "^3.13.0"
  }
}
